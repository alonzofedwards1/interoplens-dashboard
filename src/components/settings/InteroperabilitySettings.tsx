import React from "react";

/* ----------------------------------
   Interoperability Settings
----------------------------------- */

const InteroperabilitySettings: React.FC = () => {
    return (
        <div className="space-y-4 text-sm text-base-content/70">
            Retries are automatically grouped into a single logical execution.
        </div>
    );
};

export default InteroperabilitySettings;
